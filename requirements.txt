# LLM gateway
litellm[proxy]>=1.52.0

# A2A agent servers (official SDK + http-server extras)
a2a-sdk[http-server]>=0.2.0

# Agent servers + transport
fastapi>=0.115.0
starlette>=0.41.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.0.0
sse-starlette>=2.1.3
python-dotenv>=1.0.1
pyyaml>=6.0.2

# LLM SDK (agents talk OpenAI-protocol to the LiteLLM proxy)
openai>=1.50.0

# Google ADK (security agent uses ADK for cross-framework A2A demo)
google-adk[a2a]>=1.28.0

# LangGraph (performance agent uses a LangGraph state machine)
langgraph>=0.4.0
